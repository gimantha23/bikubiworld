//to get quantity bikubiworld
$sql1="SELECT * FROM `stock` WHERE `pid`=".$row['pid'].";";
        $rowsql1=mysqli_query($con,$sql1);
        $row1=mysqli_fetch_assoc($rowsql1);
        echo"<tr>
        <td>";
        $aqty=$row1['qty'];
        if($aqty <= 0 || $aqty=NULL)
          {
          echo "out of stock"."<br>";
          }
        else
          {
          echo "available: ";
          echo "".$row1['qty']."";
          }